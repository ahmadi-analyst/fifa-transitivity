# FIFA World Cup Group-Stage Transitivity (1994–2022)

Does beating the team that beat someone else actually mean you're better than them?
This project tests **transitivity** in FIFA World Cup group-stage results across the modern era (1994–2022): if Team A beats Team B and Team B beats Team C, does Team A beat Team C?

---

## Research Question

> In a round-robin group, does the win relation behave transitively — and does the pre-tournament FIFA ranking gap between A and C predict when it does?

Draws are treated as **0.5 wins** for both sides (consistent with points-table logic). A "beat" means earning a full win (1 point), not a draw.

---

## Key Findings

| Test | Result |
|---|---|
| Overall transitivity rate | **46.5%** (86/185 applicable triples) |
| Group-level bootstrap vs. 50% chance | p = 0.50 — **not significant**; 95% CI [0.37, 0.57] |
| Year-to-year variation | Chi-square p = **0.0003** — highly significant |
| Ranking-gap effect (3 buckets) | Chi-square p = **0.0002** — highly significant |
| Logistic regression (rank_gap_ac) | OR = 0.968 per rank, p = **0.0001** |

**Per-bucket breakdown** (A-vs-C pre-tournament ranking gap; CIs and p-values from group-level bootstrap, n=10,000):

| Bucket | k/n | Rate | 95% bootstrap CI | p-value |
|---|---|---|---|---|
| A better (>=10 ranks) | 51/81 | 63.0% | [0.51, 0.74] | 0.021 * |
| Similar (±10 ranks) | 14/40 | 35.0% | [0.18, 0.53] | 0.098 |
| C better (>=10 ranks) | 9/36 | 25.0% | [0.12, 0.41] | 0.001 * |

**Summary:** Overall, transitivity holds no more than chance. But conditioning on ranking splits the story sharply: when A holds a 10+ rank advantage over C, transitivity holds significantly *above* chance (63%, p = 0.026); when C holds the advantage, it falls significantly *below* chance (25%, p = 0.004). The middle bucket straddles 50% and is not significant. The 2006 World Cup was remarkably transitive (82%); 2022 was the most chaotic (21%).

---

## Definitions

- **Applicable triple**: an ordered trio (A, B, C) from the same group where A strictly beat B and B strictly beat C.
- **Transitive**: A also strictly beat C.
- **rank_gap_ac**: pre-tournament FIFA rank of A minus rank of C. Negative values mean A is ranked better (lower number = higher rank).
- **Pre-tournament ranking**: the most recent FIFA ranking snapshot published before each tournament's opening match.

---

## Data Sources

| Dataset | Source | License |
|---|---|---|
| World Cup match results | [jfjelstul/worldcup](https://github.com/jfjelstul/worldcup) | CC-BY-SA 4.0 |
| Historical FIFA rankings (1992–2024) | [Dato-Futbol/fifa-ranking](https://github.com/Dato-Futbol/fifa-ranking) | Public (scraped from FIFA) |

Pre-tournament ranking snapshots used:

| Year | Snapshot Date |
|---|---|
| 1994 | 1994-06-14 |
| 1998 | 1998-05-20 |
| 2002 | 2002-05-15 |
| 2006 | 2006-05-17 |
| 2010 | 2010-05-26 |
| 2014 | 2014-06-05 |
| 2018 | 2018-06-07 |
| 2022 | 2022-10-06 |

---

## Project Structure

```
FIFA Transitivity/
├── data/
│   ├── raw/                          # Downloaded as-is
│   │   ├── matches.csv
│   │   └── fifa_rankings_historical.csv
│   └── processed/                    # Generated by 02_process_data.py
│       ├── group_stage_matches.csv   # 372 group-stage matches, 1994–2022
│       ├── teams_with_rankings.csv   # 248 (tournament × team) ranking rows
│       └── transitivity_triples.csv # 1,488 ordered triples with outcomes
├── figures/
│   ├── 01_transitivity_by_year.png
│   ├── 02_transitivity_by_rank_gap.png
│   ├── 03_rank_gap_scatter.png
│   └── 04_group_network_sample.png
├── scripts/
│   ├── 01_download_data.py           # Fetch raw CSVs from GitHub
│   ├── 02_process_data.py            # Filter, join, build triples
│   ├── 03_analysis.py                # Statistical tests (bootstrap, chi-sq, logit)
│   └── 04_visualizations.py          # Generate all figures
└── requirements.txt
```

---

## Figures

**Figure 1 — Transitivity rate by tournament**
Red bars = below 50%; blue = above 50%. Error bars are 95% binomial CIs.
Significant tournament-to-tournament variation (chi-sq p = 0.0003).

**Figure 2 — Transitivity rate by ranking gap (A vs C)**
Three buckets: A better, similar, C better (±10-rank threshold). Error bars show 95% bootstrap CIs (group-level resampling, n=10,000); CI values printed above each bar; `*` marks buckets significantly different from 50% chance. Clear monotone trend across all three buckets (chi-sq p = 0.0002).

**Figure 3 — Rank gap vs outcome with logistic fit**
Each dot is one applicable triple (jittered vertically). The logistic curve shows P(transitive) declining as rank_gap_ac increases (i.e., as C becomes better-ranked than A).

**Figure 4 — Group match network (2022, Group C)**
Arrows show wins; dashed lines show draws. Argentina–Mexico–Poland–Saudi Arabia illustrates a partially intransitive group.

---

## Reproducing the Analysis

```bash
pip install -r requirements.txt

python scripts/01_download_data.py   # ~3 MB download from GitHub
python scripts/02_process_data.py    # builds data/processed/
python scripts/03_analysis.py        # prints statistical results to stdout
python scripts/04_visualizations.py  # saves figures/
```

All scripts are idempotent — running them twice is safe.

---

## Limitations

- **Small N**: only 185 applicable triples across 8 tournaments. Year-level and bucket-level subgroup counts are small (8–81), so per-bucket p-values are approximate.
- **Non-independence**: triples within the same group share matches. The overall and per-bucket CIs and p-values use group-level bootstrap resampling (n=10,000) to account for this; year-level chi-square still treats triples as independent.
- **Draw treatment**: calling draws 0.5 is a modelling choice. A strict "only wins count" framing would reduce N further.
- **Rankings before 1994**: the FIFA ranking system launched in 1992; the 1994 snapshot is the earliest available, so coverage is complete for this study period.
